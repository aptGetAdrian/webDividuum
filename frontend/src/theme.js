// Single source of truth for brand color values needed in JS (e.g. canvas/particle
// configs, which can't read CSS custom properties). Keep in sync with
// --accent-color / --btn-dark-bg in src/index.css.
export const ACCENT_COLOR = '#2e9cdd';
export const BRAND_NAVY = '#12353e';
